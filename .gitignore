# Virtual environment
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# Run history — regenerated by running the process
data/runs.db
data/runs.db-journal

# Uploaded invoices
data/invoices/uploads/

# Local env / secrets — but keep the documented template
.env
.env.*
!.env.example

# OS noise
.DS_Store
